<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.riotgames.readerboard.mapper.UserMapper">

    <insert id="register" parameterType="com.riotgames.readerboard.dto.UserDTO">
        INSERT INTO user ( id, matchMakerRank, rank, tier )
        VALUES ( #{id}, #{matchMakerRank}, #{rank}, #{tier})
    </insert>

    <select id="idCheck" resultType="int">
        SELECT COUNT(id)
        FROM user
        WHERE id = #{id}
    </select>

    <select id="getUserMatchMakerRank" resultType="int">
        SELECT matchMakerRank
        FROM user
        WHERE id = #{id}
    </select>

    <delete id="deleteUserProfile">
        DELETE FROM user
        WHERE id=#{id}
    </delete>

    <update id="updateUser" parameterType="com.riotgames.readerboard.dto.UserDTO">
        UPDATE user
        SET matchMakerRank = #{matchMakerRank},
        rank = #{rank},
        tier = #{tier}
        WHERE id= #{id}
    </update>

</mapper>